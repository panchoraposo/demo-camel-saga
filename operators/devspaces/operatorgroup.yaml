apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: devspaces-operatorgroup
  namespace: openshift-devspaces
spec:
  # Dev Spaces / DevWorkspace operators require cluster-wide permissions and
  # must not be configured as OwnNamespace (which happens when targetNamespaces
  # is set to the operator's namespace).

