apiVersion: noobaa.io/v1alpha1
kind: BucketClass
metadata:
  name: tempo-bucket-class
  namespace: openshift-storage
  labels:
    app: noobaa
spec:
  placementPolicy:
    tiers:
      - backingStores:
          - tempo-mcg-backingstore
        placement: Spread

