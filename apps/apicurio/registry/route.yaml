apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: apicurio
  namespace: apicurio-registry
spec:
  to:
    kind: Service
    name: apicurio-service
  port:
    targetPort: 8080
  tls:
    termination: edge
