apiVersion: registry.apicur.io/v1
kind: ApicurioRegistry
metadata:
  name: apicurio
  namespace: apicurio-registry
spec:
  configuration:
    persistence: kafkasql
    kafkasql:
      bootstrapServers: demo-cluster-kafka-bootstrap.kafka.svc:9092
