apiVersion: v1
kind: Service
metadata:
  name: allocation-metrics
  namespace: allocation
  labels:
    app: allocation
    ticketblaster.redhat.demo/component: metrics
spec:
  selector:
    serving.knative.dev/service: allocation
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
