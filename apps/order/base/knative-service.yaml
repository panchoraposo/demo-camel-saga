apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: order
  namespace: order
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "10"
      labels:
        app: order
        app.openshift.io/runtime: camel
    spec:
      containers:
        - name: user-container
          image: image-registry.openshift-image-registry.svc:5000/order/order:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: QUARKUS_PROFILE
              value: demo
            - name: QUARKUS_OIDC_AUTH_SERVER_URL
              value: https://keycloak-ingress-keycloak.apps.cluster-rvwsv.rvwsv.sandbox3546.opentlc.com/realms/ticketblaster
            - name: QUARKUS_OIDC_TOKEN_PRINCIPAL_CLAIM
              value: preferred_username
            - name: QUARKUS_OIDC_TLS_VERIFICATION
              value: none
            # Admin-only: restrict viewing all orders.
            # Quarkus resolves roles from Keycloak access tokens at realm_access.roles.
            - name: QUARKUS_OIDC_ROLES_ROLE_CLAIM_PATH
              value: realm_access/roles
            - name: QUARKUS_HTTP_AUTH_POLICY_ADMIN_ROLES_ALLOWED
              value: admin
            - name: QUARKUS_HTTP_AUTH_PERMISSION_ADMIN_PATHS
              value: /orders,/orders/*
            - name: QUARKUS_HTTP_AUTH_PERMISSION_ADMIN_POLICY
              value: admin
            - name: QUARKUS_HTTP_AUTH_PERMISSION_ADMIN_ROLES_ALLOWED
              value: admin
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "100m"
          readinessProbe:
            initialDelaySeconds: 10
            httpGet:
              path: "/q/health/ready"
              port: 8080
            timeoutSeconds: 5
            periodSeconds: 10
            failureThreshold: 3
