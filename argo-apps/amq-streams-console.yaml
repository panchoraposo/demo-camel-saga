apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-amq-streams-console
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: 'https://github.com/panchoraposo/demo-camel-saga'
    targetRevision: HEAD
    path: apps/amq-streams-console/overlays/dev
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: kafka-console
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

